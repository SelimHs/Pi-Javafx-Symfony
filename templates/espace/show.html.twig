{% extends 'base.html.twig' %}

{% block title %}Espace{% endblock %}

{% block body %}
    <h1>Espace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEspace</th>
                <td>{{ espace.idEspace }}</td>
            </tr>
            <tr>
                <th>NomEspace</th>
                <td>{{ espace.nomEspace }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ espace.adresse }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ espace.capacite }}</td>
            </tr>
            <tr>
                <th>Disponibilite</th>
                <td>{{ espace.disponibilite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ espace.prix }}</td>
            </tr>
            <tr>
                <th>Type_espace</th>
                <td>{{ espace.TypeEspace }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ espace.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_espace_index') }}">back to list</a>

    <a href="{{ path('app_espace_edit', {'idEspace': espace.idEspace}) }}">edit</a>

    {{ include('espace/_delete_form.html.twig') }}
{% endblock %}
