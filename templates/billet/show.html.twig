{% extends 'base.html.twig' %}

{% block title %}Billet{% endblock %}

{% block body %}
    <h1>Billet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdBillet</th>
                <td>{{ billet.idBillet }}</td>
            </tr>
            <tr>
                <th>Proprietaire</th>
                <td>{{ billet.proprietaire }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ billet.prix }}</td>
            </tr>
            <tr>
                <th>DateAchat</th>
                <td>{{ billet.dateAchat ? billet.dateAchat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ billet.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_billet_index') }}">back to list</a>

    <a href="{{ path('app_billet_edit', {'idBillet': billet.idBillet}) }}">edit</a>

    {{ include('billet/_delete_form.html.twig') }}
{% endblock %}
