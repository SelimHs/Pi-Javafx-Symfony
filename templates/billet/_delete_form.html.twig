<form method="post" action="{{ path('app_billet_delete', {'idBillet': billet.idBillet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ billet.idBillet) }}">
    <button class="btn">Delete</button>
</form>
