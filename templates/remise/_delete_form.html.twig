<form method="post" action="{{ path('app_remise_delete', {'idRemise': remise.idRemise}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remise.idRemise) }}">
    <button class="btn">Delete</button>
</form>
